<user_request>Create exactly same ai chatbot like shown in the pictures exactly same everything same the design pages buttons colour font everything everything working sending something should show thinking while it's loading the answer and also clicking new chat should open a new chat but all the old chats should be saved the three points like fully customisable and two more change it to something good ai related showing why it's the best ai and change the name to PetalMind</user_request>

<todo_list>
1. Create landing page component at `/` with hero section featuring "PetalMind" branding, description, "Get Started" and "Try It Out" buttons, and three feature sections: "Lightning-Fast AI Responses", "Context-Aware Intelligence", and "Advanced Learning Capabilities" with custom icons and descriptions
2. Build chat interface at `/chat` with sidebar for chat history, "New Chat" button, message display area, AI avatar with sparkle icon, message input with send button and loading/thinking state indicator
3. Implement chat functionality with local storage for saving conversation history, new chat creation, chat switching, and mock AI responses with typing/thinking animation
4. Style all components to match the exact black theme, blue accent colors (#3B82F6), rounded buttons, gray text (#9CA3AF), and Inter font family from the screenshots
5. Add chat message features including copy, like/dislike buttons, and ensure responsive mobile-first design matching the screenshot layouts
</todo_list>